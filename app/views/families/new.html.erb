<h1>グループ新規作成画面</h1>

<%= form_with model: @family, local: true do |f| %>
  <%= f.label :name, "グループ名" %>
  <%= f.text_field :name %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.submit "グループを作成する" %>
<% end %>
